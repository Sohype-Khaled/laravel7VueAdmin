import AuthMiddleware from "@/js/routes/middleware/AuthMiddleware";
import ACLMiddleware from "@/js/routes/middleware/ACLMiddleware";
import CanMiddleware from "@/js/routes/middleware/CanMiddleware";

export default {
    ACLMiddleware,
    AuthMiddleware,
    CanMiddleware
}
